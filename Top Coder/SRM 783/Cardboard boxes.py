# 1000 point problem

class CardboardBoxes:
    """define the length width and height of the top and lower"""
    def topflap(self, S):
        """Initializing the object"""
        CardboardBoxes = {'length': 50, 'width': 21}
        self.S = S
        if topflap >= 10.5:
            print('This is the correct size for the top')
        elif topflap < 10.5:
            print('This is not the correct size for the top flap')
        else:
            CardboardBoxes = 70
    def bottomflap(self):
        """Setting the sizes"""
        if bottomflap == 10.5:
            print('This is the correct size for the bottom flap')
        elif bottomflap < 10.5:
            print('This is the incorrect size for the bottom flap')
        return boxes
        
boxes_cut = CardboardBoxes('flaps')

print(boxes_cut.flaps + 'the size of the overall flaps')
boxes_cut.flap()
