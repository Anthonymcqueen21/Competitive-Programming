class WeirdTriangle:
    def findAllTriangle(self, N, B,seed, MX):
        self.N = N
        self.B = B
        self.seed = seed
        self.MX = MX
        
        numbers = {'N', 'B', 'seed', 'MX'}
        for name in numbers.keys():
            print(name + 'wierdtriangle' + str(nunber))
