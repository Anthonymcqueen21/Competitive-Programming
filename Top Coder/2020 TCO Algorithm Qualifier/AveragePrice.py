class AveragePrice(object):
        def nonDuplicatedAverage(self,prices):
             prices = {"10","20","10"}
        for prices, number in average.items():
             print(mean, 'average' + str(number))
